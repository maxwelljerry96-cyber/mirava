'use client';import {useState} from 'react';
export function NewsletterForm({compact=false}:{compact?:boolean}){const[status,setStatus]=useState(''),[loading,setLoading]=useState(false);async function action(fd:FormData){setLoading(true);const r=await fetch('/api/newsletter',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email:fd.get('email')})});const d=await r.json();setStatus(r.ok?d.message:d.error);setLoading(false)}return <form action={action} className={`newsletter-form ${compact?'compact':''}`}><input name="email" type="email" placeholder="Enter your email address" required/><button className="button button-primary" disabled={loading}>{loading?'Joining…':'Subscribe'}</button>{status&&<p className="form-status">{status}</p>}</form>}
