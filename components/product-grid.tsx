'use client';import {useMemo,useState} from 'react';import {ProductCard} from '@/components/product-card';import type {Product} from '@/types';
export function ProductGrid({products,filters=true}:{products:Product[];filters?:boolean}){const cats=['All',...new Set(products.map(p=>p.category))],[cat,setCat]=useState('All'),[sort,setSort]=useState('featured');const list=useMemo(()=>{const a=cat==='All'?products:products.filter(p=>p.category===cat);return [...a].sort((x,y)=>sort==='low'?x.price_minor-y.price_minor:sort==='high'?y.price_minor-x.price_minor:Number(y.featured)-Number(x.featured))},[products,cat,sort]);return <>{filters&&<div className="catalog-controls"><div className="filter-pills">{cats.map(c=><button key={c} className={c===cat?'active':''} onClick={()=>setCat(c)}>{c}</button>)}</div><select value={sort} onChange={e=>setSort(e.target.value)}><option value="featured">Featured</option><option value="low">Price: low to high</option><option value="high">Price: high to low</option></select></div>}<div className="product-grid">{list.map(p=><ProductCard key={p.id} product={p}/>)}</div></>}
