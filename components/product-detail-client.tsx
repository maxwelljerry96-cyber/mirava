'use client';import {useState} from 'react';import {Minus,Plus} from 'lucide-react';import {useCart} from '@/components/cart-provider';import type {Product} from '@/types';
export function ProductDetailClient({product}:{product:Product}){const[q,setQ]=useState(1),{addProduct}=useCart();return <div className="product-purchase"><div className="quantity-control"><button onClick={()=>setQ(Math.max(1,q-1))}><Minus/></button><b>{q}</b><button onClick={()=>setQ(Math.min(product.stock,q+1))}><Plus/></button></div><button className="button button-primary" onClick={()=>addProduct(product,q)} disabled={!product.stock}>{product.stock?'Add to cart':'Out of stock'}</button></div>}
