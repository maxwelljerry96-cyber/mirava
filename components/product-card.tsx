'use client';import Image from 'next/image';import Link from 'next/link';import {useCart} from '@/components/cart-provider';import {formatMoney} from '@/lib/catalog';import type {Product} from '@/types';
export function ProductCard({product}:{product:Product}){const{addProduct}=useCart();return <article className="product-card"><Link href={`/products/${product.slug}`} className="product-thumb"><Image src={product.image_url} alt={product.name} width={800} height={600}/></Link><div className="product-info"><span className="product-category">{product.category}</span><Link href={`/products/${product.slug}`}><h3>{product.name}</h3></Link><p>{product.short_description}</p></div><div className="product-meta"><strong>{formatMoney(product.price_minor,product.currency)}</strong><button className="add-button" onClick={()=>addProduct(product)} disabled={!product.stock}>{product.stock?'Add to cart':'Sold out'}</button></div></article>}
