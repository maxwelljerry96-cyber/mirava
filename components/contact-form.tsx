'use client';import {useState} from 'react';
export function ContactForm(){const[status,setStatus]=useState(''),[loading,setLoading]=useState(false);async function action(fd:FormData){setLoading(true);const r=await fetch('/api/contact',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(Object.fromEntries(fd.entries()))});const d=await r.json();setStatus(r.ok?d.message:d.error);setLoading(false)}return <form className="form-card" action={action}><h2>Send Us a Message</h2><div className="field-grid"><label>Full name<input name="fullName" required/></label><label>Email<input type="email" name="email" required/></label></div><label>Subject<input name="subject" required/></label><label>Message<textarea name="message" rows={6} required/></label><button className="button button-primary" disabled={loading}>{loading?'Sending…':'Send message'}</button>{status&&<p className="form-status">{status}</p>}</form>}
