import {requireAdmin} from '@/lib/auth';import {createAdminClient} from '@/lib/supabase/admin';import {formatMoney} from '@/lib/catalog';export default async function Page(){await requireAdmin();const s=createAdminClient();const[{count:p},{count:o},{count:m},{data:r}]=await Promise.all([s.from('products').select('*',{count:'exact',head:true}).eq('active',true),s.from('orders').select('*',{count:'exact',head:true}),s.from('contact_messages').select('*',{count:'exact',head:true}).eq('resolved',false),s.from('orders').select('total_minor').eq('payment_status','paid')]);return <><div className="admin-title"><span className="eyebrow">Overview</span><h1>Store Dashboard</h1></div><div className="admin-stats"><article><span>Active products</span><b>{p||0}</b></article><article><span>Orders</span><b>{o||0}</b></article><article><span>Open messages</span><b>{m||0}</b></article><article><span>Paid revenue</span><b>{formatMoney(r?.reduce((a,x)=>a+Number(x.total_minor),0)||0)}</b></article></div><article className="admin-card"><h2>Launch Checklist</h2><ol><li>Run the Supabase SQL migration.</li><li>Add environment variables in Vercel.</li><li>Set one profile role to admin.</li><li>Add the Paystack webhook URL.</li></ol></article></>}
