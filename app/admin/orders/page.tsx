import {requireAdmin} from '@/lib/auth';import {createAdminClient} from '@/lib/supabase/admin';import {updateOrder} from '@/app/admin/actions';import {formatMoney} from '@/lib/catalog';const statuses=['pending_payment','paid','processing','shipped','delivered','cancelled','refunded'];export default async function Page(){await requireAdmin();const s=createAdminClient(),{data}=await s.from('orders').select('*,order_items(*)').order('created_at',{ascending:false});return <><div className="admin-title"><h1>Order Management</h1></div><div className="table-wrap"><table><thead><tr><th>Reference</th><th>Customer</th><th>Total</th><th>Payment</th><th>Status</th></tr></thead><tbody>{data?.map(o=><tr key={o.id}><td>{o.reference}</td><td>{o.full_name}<small>{o.email}</small></td><td>{formatMoney(o.total_minor,o.currency)}</td><td>{o.payment_status}</td><td><form action={updateOrder}><input type="hidden" name="id" value={o.id}/><select name="status" defaultValue={o.status}>{statuses.map(x=><option key={x}>{x}</option>)}</select><button>Update</button></form></td></tr>)}</tbody></table></div></>}
