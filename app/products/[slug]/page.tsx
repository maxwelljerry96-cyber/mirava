import Image from 'next/image';import Link from 'next/link';import {notFound} from 'next/navigation';import {ProductDetailClient} from '@/components/product-detail-client';import {ProductGrid} from '@/components/product-grid';import {getProductBySlug,getProducts} from '@/lib/products';import {formatMoney} from '@/lib/catalog';
export default async function Page({params}:{params:Promise<{slug:string}>}){const{slug}=await params,p=await getProductBySlug(slug);if(!p)notFound();const all=await getProducts();return <><section className="product-detail container"><Link href="/products" className="text-link">← Back to products</Link><div className="product-detail-grid"><Image src={p.image_url} alt={p.name} width={1200} height={900}/><div><span className="eyebrow">{p.category}</span><h1>{p.name}</h1><p className="lead">{p.short_description}</p><strong className="price">{formatMoney(p.price_minor,p.currency)}</strong><p>{p.description}</p><p>{p.stock} bottles available</p><ProductDetailClient product={p}/><div className="detail-boxes"><article><h3>Ingredients</h3><ul>{p.ingredients.map(x=><li key={x}>{x}</li>)}</ul></article><article><h3>Why you’ll love it</h3><ul>{p.benefits.map(x=><li key={x}>{x}</li>)}</ul></article></div></div></div></section><section className="section soft"><div className="container"><div className="heading"><h2>You May Also Like</h2></div><ProductGrid products={all.filter(x=>x.id!==p.id).slice(0,3)} filters={false}/></div></section></>}
