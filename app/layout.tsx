import type {Metadata} from 'next';import '@/app/globals.css';import {CartProvider} from '@/components/cart-provider';import {CartDrawer} from '@/components/cart-drawer';import {Header} from '@/components/header';import {Footer} from '@/components/footer';import {getProducts} from '@/lib/products';
export const dynamic='force-dynamic';export const revalidate=0;export const metadata:Metadata={title:{default:'FRUTÉO · Pure Fruit Elixirs',template:'%s · FRUTÉO'},description:'A premium fruit juice store with real products, accounts, checkout, orders and administration.'};
export default async function Layout({children}:{children:React.ReactNode}){const products=await getProducts();return <html lang="en"><body><CartProvider><Header products={products}/><CartDrawer/><main>{children}</main><Footer/></CartProvider></body></html>}
